fn main() {

    // immutable vs mutable variables

    let mut x = 5;
    x = 9; // This is not possible in Rust. This is because Rust makes all variables immutable by default.

    // let mut y = 10;
    // y = 10;

    println!("Hello, world!");
}
